marketData {
    publishAddress = "tcp://localhost:5557"
    symbols = [ MSFT, GOOG, YHOO, IBM ]
    sendInterval = 100
}

messageBroker {
    xsubAddress = "tcp://localhost:5557"
    xpubAddress = "tcp://localhost:5558"
}

algorithm {
    subscribeAddress = "tcp://localhost:5558"
    tradeReportAddress = "" // TODO
}

tradeReport {
    subscribeAddress = "tcp://localhost:5557"
}